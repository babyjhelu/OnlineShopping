<div class="page-header">
  <%= link_to products_path(:category => @category.id), class: 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Back
  <% end %>
  <h1><%= @category.category_name %> New product</h1>
</div>

<%= render 'form' %>
